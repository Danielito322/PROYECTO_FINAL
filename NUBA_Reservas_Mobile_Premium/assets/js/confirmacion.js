(function(){const N=window.NUBA;if(!N)return;const booking=N.get('lastBooking',null)||N.get('bookings',[])[0];const root=document.getElementById('confirmation-root');if(!root)return;if(!booking){root.innerHTML='<div class="empty">Aún no tienes una reserva confirmada.</div>';return}const cells=Array.from({length:81},(_,i)=>`<i style="${(i%7===0||i%11===0||[1,2,9,10,11,18,19,20,60,61,62,69,70,71].includes(i))?'opacity:1':''}"></i>`).join('');root.innerHTML=`<header class="topbar"><div><div class="eyebrow">Reserva confirmada</div><h1>Todo está listo</h1><p>Guarda tu código para el ingreso o validación.</p></div></header><section class="card qr-wrap"><div class="success-ring">${N.icon('check','lg')}</div><h2 style="font-size:20px;margin:0 0 5px">Pago exitoso</h2><p style="font-size:11px;color:var(--muted);margin:0">Código de reserva: <strong style="color:var(--text)">${booking.id}</strong></p><div class="qr-box">${cells}<div class="qr-center">${N.icon('qr','lg')}</div></div><span class="badge">${booking.category||'Reserva'} · ${booking.date} · ${booking.time}</span><p style="font-size:11px;color:var(--muted);line-height:1.5">Muestra este QR al proveedor. Tu reserva está guardada en “Mis reservas”.</p></section><div class="btn-row" style="margin-top:14px"><a class="ghost-btn" href="mis-reservas.html">Mis reservas</a><a class="primary-btn" href="../index.html">Ir al inicio</a></div>`;})();
