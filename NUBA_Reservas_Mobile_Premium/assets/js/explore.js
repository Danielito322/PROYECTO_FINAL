(function(){const N=window.NUBA;if(!N)return;const list=document.getElementById('venue-list'),title=document.getElementById('explore-title'),sub=document.getElementById('explore-subtitle'),search=document.getElementById('venue-search');let cat=N.qs('categoria')||'todos';
  function render(){const term=(search?.value||'').toLowerCase();const rows=N.venues.filter(v=>(cat==='todos'||v.category===cat)&&(`${v.name} ${v.type} ${v.address}`.toLowerCase().includes(term)));if(title){title.textContent=cat==='todos'?'Explorar lugares':N.categories[cat].title;sub.textContent=cat==='todos'?'Elige una categoría y reserva sin llamadas.':N.categories[cat].subtitle}
  list.innerHTML=rows.length?rows.map(v=>`<a class="venue-card card" href="detalle.html?id=${v.id}"><div class="venue-img"><img src="${v.photo}" alt="${v.name}"></div><div class="venue-info"><div class="venue-line"><span class="venue-type">${v.label}</span><span class="rating">★ ${v.rating}</span></div><strong class="venue-title">${v.name}</strong><p class="venue-meta">${v.type}<br>${v.address}</p><div class="price">${N.money(v.price)} <small>${v.unit}</small></div></div></a>`).join(''):'<div class="empty">No encontramos locales con ese filtro.</div>';
  document.querySelectorAll('[data-filter]').forEach(b=>b.classList.toggle('active',b.dataset.filter===cat));}
  document.querySelectorAll('[data-filter]').forEach(b=>b.addEventListener('click',()=>{cat=b.dataset.filter;render()}));search?.addEventListener('input',render);render();})();
