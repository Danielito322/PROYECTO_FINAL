(function(){const N=window.NUBA;if(!N)return;const v=N.venueById(N.qs('id'));const root=document.getElementById('shop-root');if(!root)return;if(!v.shop){root.innerHTML=`<header class="topbar"><div><div class="eyebrow">${v.name}</div><h1>Tienda no disponible</h1><p>Este local no tiene productos asociados por ahora.</p></div><button class="icon-btn" data-back>${N.icon('back')}</button></header><div class="empty">Puedes continuar con la reserva del servicio.<br><br><a class="primary-btn" href="reservar.html?id=${v.id}">Reservar ahora</a></div>`;return}
root.innerHTML=`<header class="topbar"><div><div class="eyebrow">Tienda del local</div><h1>${v.name}</h1><p>Productos opcionales para tu reserva.</p></div><a class="icon-btn" href="carrito.html">${N.icon('bag')}</a></header><div id="product-list" style="display:grid;gap:11px"></div>`;const list=root.querySelector('#product-list');list.innerHTML=v.products.map(p=>`<article class="venue-card card" style="grid-template-columns:92px 1fr"><div class="venue-img" style="height:98px"><img src="${p.photo}" alt="${p.name}"></div><div class="venue-info"><span class="venue-type">Producto del local</span><strong class="venue-title">${p.name}</strong><p class="venue-meta">Disponible para recoger junto con tu reserva.</p><div class="venue-line"><strong class="price">${N.money(p.price)}</strong><button class="mini-btn" data-product="${p.id}">Agregar</button></div></div></article>`).join('');list.querySelectorAll('[data-product]').forEach(b=>b.onclick=()=>{const p=v.products.find(x=>x.id===b.dataset.product);const cart=N.get('cart',[]);const ex=cart.find(x=>x.id===p.id&&x.venueId===v.id);if(ex)ex.qty++;else cart.push({...p,venueId:v.id,venue:v.name,qty:1});N.set('cart',cart);N.toast('Producto añadido al carrito.');b.textContent='Añadido';setTimeout(()=>b.textContent='Agregar',900)});})();
