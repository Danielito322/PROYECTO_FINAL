(function(){const N=window.NUBA;if(!N)return;const v=N.venueById(N.qs('id'));document.title=v.name+' · NUBA';
  const root=document.getElementById('venue-detail');if(!root)return;
  root.innerHTML=`<section class="detail-cover"><img src="${v.photo}" alt="${v.name}"><button class="icon-btn detail-back" data-back>${N.icon('back')}</button><div class="detail-overlay"><span class="badge">${v.label}</span><h1>${v.name}</h1><p>${v.type} · ${v.address}</p></div></section><section class="detail-body"><div class="section-head"><div><h3>Disponible para reservar</h3><p>${v.availability}</p></div><span class="rating">★ ${v.rating}</span></div><div class="info-grid"><div class="card info-stat"><strong>${N.money(v.price)}</strong><span>${v.unit}</span></div><div class="card info-stat"><strong>${v.distance}</strong><span>de distancia</span></div><div class="card info-stat"><strong>4.8/5</strong><span>reseñas</span></div></div><div class="section-head"><h3>Conoce el lugar</h3></div><div class="gallery-row">${[v.photo,...v.gallery].map(p=>`<img src="${p}" alt="Galería ${v.name}">`).join('')}</div>${v.shop?`<a class="store-spot card" href="tienda.html?id=${v.id}">${N.icon('bag')}<div><h4>Tienda del local</h4><p>Este negocio ofrece productos que puedes añadir a tu reserva.</p></div>${N.icon('chevron','sm')}</a>`:`<div class="store-spot card"><div style="width:28px"></div><div><h4>Reserva sin productos adicionales</h4><p>Este local no tiene tienda activa; puedes continuar con tu reserva.</p></div></div>`}<div class="section-head"><h3>Información del servicio</h3></div><div class="card card-pad"><p style="font-size:12px;color:var(--muted);line-height:1.65;margin:0">Reserva de forma clara y segura. Los horarios se actualizan al confirmar y recibirás un código QR para ingresar o validar tu servicio.</p></div><div class="sticky-action"><a class="primary-btn full" href="reservar.html?id=${v.id}">Elegir fecha y horario ${N.icon('arrow')}</a></div></section>`;
})();
