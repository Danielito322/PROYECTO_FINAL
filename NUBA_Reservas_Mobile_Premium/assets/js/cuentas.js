(function(){const N=window.NUBA;if(!N)return;const root=document.getElementById('accounts-root');if(!root)return;const current=N.currentRole();const items=[['cliente','Cliente','Reserva servicios, paga y guarda tus códigos QR.','user','perfil.html'],['proveedor','Proveedor','Publica tu local, gestiona horarios, productos y reservas.','store','negocio.html'],['administrador','Administrador','Supervisa usuarios, locales, categorías y reportes.','admin','admin.html']];root.innerHTML=`<header class="topbar"><div><div class="eyebrow">Accesos</div><h1>Elige tu cuenta</h1><p>En la demostración puedes cambiar de rol cuando necesites.</p></div><button class="icon-btn" data-back>${N.icon('back')}</button></header>${items.map(([id,title,desc,ico,next])=>`<article class="role-card card"><div class="role-visual">${N.icon(ico,'lg')}</div><div><h3>${title}</h3><p>${desc}</p></div><button class="secondary-btn" data-role="${id}" data-next="${next}">${current===id?'Activa':'Usar'}</button></article>`).join('')}`;})();
