(function(){
  const list=document.getElementById('booking-list');if(!list)return;const root=APP_CONFIG.root;const demo={id:'NUBA-DEMO',venueName:'Arena Norte',venueImage:NUBA_DATA.venues[0].image,venueType:'Canchas',date:'sábado, 5 de julio',time:'19:00',total:45,status:'Pendiente de pago'};const bookings=NUBA_STORE.bookings();const items=bookings.length?bookings:[demo];list.innerHTML=items.map(b=>`<article class="reservation-card"><img src="${b.venueImage}" alt="${b.venueName}" style="width:58px;height:58px;border-radius:14px;object-fit:cover"><div style="flex:1"><strong style="font-size:12px;display:block">${b.venueName}</strong><span style="font-size:10px;color:var(--muted);font-weight:650;display:block;margin-top:3px">${b.date} · ${b.time}<br>${b.status}</span></div><div style="text-align:right"><b style="display:block;font-size:11px">${money(b.total)}</b>${b.status==='Confirmada'?`<a class="btn btn-ghost btn-small" style="display:inline-block;margin-top:7px" href="${root}pages/confirmacion.html?code=${b.id}">QR</a>`:''}</div></article>`).join('');
})();
