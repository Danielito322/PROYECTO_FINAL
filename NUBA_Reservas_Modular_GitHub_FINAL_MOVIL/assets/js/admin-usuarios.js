document.addEventListener('DOMContentLoaded',()=>{const h=document.getElementById('admin-users-app');function render(){const a=NUBA_STORE.getAdmin();h.innerHTML=`<section class="page-content"><header class="topbar"><div><div class="eyebrow">Gestión de acceso</div><h1>Usuarios</h1><p>Consulta perfiles y controla accesos.</p></div><a class="icon-btn" href="admin.html">${NUBA.icon('arrowLeft')}</a></header><div class="venue-list">${a.users.map(u=>`<article class="approval-row"><span style="width:44px;height:44px;display:grid;place-items:center;border-radius:14px;background:rgba(104,213,255,.12);color:var(--cyan);font-weight:800">${u.name.charAt(0)}</span><div style="flex:1"><strong style="font-size:12px">${u.name}</strong><span style="display:block;color:var(--muted);font-size:10px;margin-top:3px">${u.email} · ${u.role}</span><span class="badge ${u.status==='active'?'green':'amber'}" style="margin-top:6px">${u.status==='active'?'Activo':'En revisión'}</span></div><button class="btn btn-secondary btn-sm" data-user="${u.id}">${u.status==='active'?'Bloquear':'Activar'}</button></article>`).join('')}</div></section>`;NUBA.refresh(h)}h.addEventListener('click',e=>{const b=e.target.closest('[data-user]');if(!b)return;const a=NUBA_STORE.getAdmin();a.users=a.users.map(u=>u.id===b.dataset.user?{...u,status:u.status==='active'?'blocked':'active'}:u);NUBA_STORE.setAdmin(a);NUBA.toast('Estado del usuario actualizado.');render()});render()});
