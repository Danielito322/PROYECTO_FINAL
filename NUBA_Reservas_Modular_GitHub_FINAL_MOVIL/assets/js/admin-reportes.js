document.addEventListener('DOMContentLoaded',()=>{const h=document.getElementById('admin-reports-app');function render(){const a=NUBA_STORE.getAdmin();h.innerHTML=`<section class="page-content"><header class="topbar"><div><div class="eyebrow">Calidad y soporte</div><h1>Reportes</h1><p>Da seguimiento a incidencias de usuarios y negocios.</p></div><a class="icon-btn" href="admin.html">${NUBA.icon('arrowLeft')}</a></header><div class="venue-list">${a.reports.map(r=>`<article class="approval-row"><span style="width:44px;height:44px;display:grid;place-items:center;border-radius:14px;background:rgba(255,151,200,.12);color:var(--pink)">${NUBA.icon('shield')}</span><div style="flex:1"><span class="badge ${r.status==='open'?'amber':'green'}">${r.status==='open'?'Abierto':'Resuelto'}</span><strong style="display:block;font-size:12px;margin-top:6px">${r.title}</strong><span style="display:block;color:var(--muted);font-size:10px;margin-top:3px">${r.place}</span></div>${r.status==='open'?`<button class="btn btn-success btn-sm" data-resolve="${r.id}">Resolver</button>`:''}</article>`).join('')}</div></section>`;NUBA.refresh(h)}h.addEventListener('click',e=>{const b=e.target.closest('[data-resolve]');if(!b)return;const a=NUBA_STORE.getAdmin();a.reports=a.reports.map(r=>r.id===b.dataset.resolve?{...r,status:'resolved'}:r);NUBA_STORE.setAdmin(a);NUBA.toast('Reporte marcado como resuelto.');render()});render()});
