document.addEventListener('DOMContentLoaded',()=>{const h=document.getElementById('provider-products-app');let p=NUBA_STORE.getProvider(),image='';function render(){h.innerHTML=`<section class="page-content"><header class="topbar"><div><div class="eyebrow">Tienda del local</div><h1>Productos</h1><p>Publica imágenes, precio, stock y detalles.</p></div><a class="icon-btn" href="negocio.html">${NUBA.icon('arrowLeft')}</a></header>${NUBA_PROVIDER.tabs('productos')}<div class="section-head"><h3>Catálogo publicado</h3><p>${p.products.length} productos</p></div><div class="venue-list">${p.products.length?p.products.map(x=>`<article class="stock-row"><img src="${x.image}" alt="${x.name}"><div class="stock-copy"><strong>${x.name}</strong><span>${NUBA.money(x.price)} · Stock ${x.stock}</span><span>${x.description||x.category||''}</span></div><div class="stock-actions"><button data-stock="${x.id}" data-delta="-1">${NUBA.icon('minus')}</button><button data-stock="${x.id}" data-delta="1">${NUBA.icon('plus')}</button></div></article>`).join(''):`<div class="empty-state">${NUBA.icon('package')}<h3>Aún no hay productos</h3><p>Agrega una imagen, nombre, descripción, precio y stock para activar tu tienda.</p></div>`}</div><button class="btn btn-primary btn-block" id="open-modal" style="margin-top:15px">${NUBA.icon('plus')} Agregar producto</button></section><div id="product-modal" class="modal-backdrop"><div class="modal"><div class="modal-handle"></div><div class="modal-title"><h3>Nuevo producto</h3><button class="close-modal" data-close>${NUBA.icon('x')}</button></div><form id="product-form"><div class="upload-box" id="prod-preview">${NUBA.icon('image','xl')}<span>Vista previa de la foto</span></div><div class="form-field" style="margin-top:12px"><label class="form-label">Foto del producto</label><input class="input" id="prod-image-url" placeholder="URL de la imagen"></div><div class="form-field"><label class="form-label">O subir fotografía</label><input class="input" id="prod-file" type="file" accept="image/*"></div><div class="form-field"><label class="form-label">Nombre del producto</label><input class="input" id="prod-name" required placeholder="Ej. Balón profesional"></div><div class="form-field"><label class="form-label">Descripción</label><textarea class="textarea" id="prod-desc" required placeholder="¿Qué incluye o para qué sirve?"></textarea></div><div class="form-grid-2"><div class="form-field"><label class="form-label">Precio</label><input class="input" id="prod-price" required type="number" min="1" placeholder="0"></div><div class="form-field"><label class="form-label">Stock</label><input class="input" id="prod-stock" required type="number" min="0" placeholder="0"></div></div><div class="form-field"><label class="form-label">Categoría</label><input class="input" id="prod-cat" required placeholder="Ej. Accesorios"></div><button class="btn btn-primary btn-block">Guardar producto</button></form></div></div>`;NUBA.refresh(h)}function preview(src){image=src;const x=h.querySelector('#prod-preview');x.innerHTML=src?`<img src="${src}" alt="Vista previa">`:`${NUBA.icon('image','xl')}<span>Vista previa de la foto</span>`;NUBA.refresh(x)}h.addEventListener('click',e=>{if(e.target.closest('#open-modal'))h.querySelector('#product-modal').classList.add('show');if(e.target.closest('[data-close]')||e.target.id==='product-modal')h.querySelector('#product-modal').classList.remove('show');const b=e.target.closest('[data-stock]');if(b){p.products=p.products.map(x=>x.id===b.dataset.stock?{...x,stock:Math.max(0,x.stock+(+b.dataset.delta))}:x);NUBA_STORE.setProvider(p);render()}});h.addEventListener('input',e=>{if(e.target.id==='prod-image-url')preview(e.target.value)});h.addEventListener('change',e=>{if(e.target.id==='prod-file'&&e.target.files[0]){const r=new FileReader();r.onload=()=>preview(r.result);r.readAsDataURL(e.target.files[0])}});h.addEventListener('submit',e=>{if(e.target.id!=='product-form')return;e.preventDefault();if(!image){NUBA.toast('Agrega una foto o URL para el producto.');return}p.products.push({id:'pp'+Date.now(),venue:'provider',name:h.querySelector('#prod-name').value,description:h.querySelector('#prod-desc').value,price:+h.querySelector('#prod-price').value,stock:+h.querySelector('#prod-stock').value,category:h.querySelector('#prod-cat').value,image});NUBA_STORE.setProvider(p);NUBA.toast('Producto publicado en la tienda.');render()});render()});
