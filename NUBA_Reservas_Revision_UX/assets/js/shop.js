document.addEventListener('DOMContentLoaded',()=>{const h=document.getElementById('shop-app');const venueId=NUBA.qs('venue')||'arena-sur';const v=NUBA_DATA.getVenue(venueId);let cat='todos';function items(){return [...NUBA_DATA.products,...NUBA_STORE.getProvider().products.filter(p=>p.id.startsWith('pp'))].filter(p=>p.venue===venueId||venueId==='provider'||(venueId==='arena-sur'&&p.venue==='provider'))}function render(){const all=items();const cats=['todos',...new Set(all.map(p=>p.category))];const shown=all.filter(p=>cat==='todos'||p.category===cat);h.innerHTML=`<section class="page-content"><header class="topbar"><div><div class="eyebrow">Tienda del local</div><h1>${v.name}</h1><p>Productos que puedes añadir a tu experiencia.</p></div><a class="icon-btn" href="carrito.html" aria-label="Carrito">${NUBA.icon('bag')}</a></header><div class="chips">${cats.map(x=>`<button class="chip ${cat===x?'active':''}" data-cat="${x}">${x==='todos'?'Todo':x}</button>`).join('')}</div><div class="section-head"><h3>Productos disponibles</h3><a class="text-link" href="carrito.html">Ver carrito (${NUBA_STORE.cartCount()})</a></div><div class="product-grid">${shown.length?shown.map(p=>`<article class="product-card card"><img src="${p.image}" alt="${p.name}"><div class="product-body"><h3>${p.name}</h3><p>${p.description||p.category}</p><div class="product-bottom"><strong>${NUBA.money(p.price)}</strong><button class="product-add" data-add="${p.id}" aria-label="Agregar ${p.name}">${NUBA.icon('plus')}</button></div></div></article>`).join(''):`<div class="empty-state" style="grid-column:1/-1">${NUBA.icon('bag')}<h3>Este local no tiene productos</h3><p>La tienda solo aparece cuando el proveedor publica artículos.</p></div>`}</div></section>`;NUBA.refresh(h)}h.addEventListener('click',e=>{const b=e.target.closest('[data-add]');if(b){NUBA_STORE.addCart(b.dataset.add);NUBA.toast('Producto agregado al carrito.');render()}const c=e.target.closest('[data-cat]');if(c){cat=c.dataset.cat;render()}});render()});
